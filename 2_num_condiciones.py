def numeros_divisibles(lista):
       salida = []
       for num in lista:
               if num % 5 == 0 and num <= 600:
                       if num <= 1000:
                               salida.append(num)
       else:
               return salida
               return salida
               lista = [24, 150, 300, 660, 295, 1050, 50]
               resultados = numeros_divisibles(lista)
               print(resultados)

